services:
  - type: web
    name: ifrs-ai-backend
    env: python
    envVars:
      - key: NIXPACKS_PYTHON_VERSION
        value: "3.11"
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
