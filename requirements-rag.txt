# RAG (Retrieval-Augmented Generation) — optional heavy dependencies
# Install with: pip install -r requirements-rag.txt
# WARNING: torch alone is ~500MB; NVIDIA CUDA packages add ~2GB.
# Only install on instances with >= 4GB RAM.
# The app works without these — set DISABLE_RAG=true on Render.

chromadb==0.5.23
sentence-transformers==3.3.1
langchain==0.3.7
langchain-community==0.3.7
langchain-anthropic==0.3.0
